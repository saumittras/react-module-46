/**
 * 1. Use form tag and onSubmit handler with event () from e access e.target.[name of input field].value
 * 2. Controlled element: use individual field state for each input field
 * 3. Contrilled element: one object holding in a stae holding all the  field values
 * 4. Uncontrolled element: useRef(0)
 *
 */
